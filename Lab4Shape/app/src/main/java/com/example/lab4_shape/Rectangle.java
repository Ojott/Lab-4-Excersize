package com.example.lab4_shape;

public class Rectangle extends Shape{
    private int x, y;

    public Rectangle(int x, int y) {
        super(x, y);
        this.x = x;
        this.y = y;
    }
}
